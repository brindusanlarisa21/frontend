# Baked into the bundle at build time — must be the deployed API URL, with /api.
# Local development:
VITE_API_URL=https://localhost:7213/api
# Production example:
# VITE_API_URL=https://tripsplit.example.com/api
